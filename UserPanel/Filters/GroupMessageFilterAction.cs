using UserPanel.Models.Messages;

namespace UserPanel.Filters
{
    public class GroupMessageFilterAction : MessageFilterAction
    {
        public GroupMessageFilterAction(GroupFormMessages message) : base(message)
        {
        }
    }
}
